<?xml version="1.0" encoding="UTF-8"?>

<!-- 마이바티스 3 Mapper DTD -->
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="calendar.dao.face.CalendarDao">
	<insert id="insertMyCal" parameterType="calendar.dto.Calendar">
		INSERT INTO my_calender (cal_no, user_no, cal_title, start_date, end_date,  cal_place, cal_memo)
		VALUES(seq_my_calender.nextval, 1, #{calTitle}, #{startDate}, #{endDate},  #{calPlace}, #{calMemo})
	</insert>
</mapper>