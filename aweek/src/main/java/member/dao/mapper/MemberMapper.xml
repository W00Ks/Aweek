<?xml version="1.0" encoding="UTF-8"?>

<!-- 마이바티스 3 Mapper DTD -->
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member.dao.face.MemberDao">
	
	<insert id="insert" parameterType="member.dto.Member">
		INSERT INTO member ( user_no, user_id, user_pw, user_email, user_phone, user_name, user_address, user_birth, user_join )
		VALUES ( member_seq.nextval, #{userId}, #{userPw}, #{userEmail}, #{userPhone}, '이름', '주소', '1989-11-08', SYSDATE )
	</insert>
	
	<select id="selectCntByIdPw" resultType="int" parameterType="member.dto.Member">
		SELECT count(*) FROM member
		WHERE user_id = #{userId} AND user_pw = #{userPw}
	</select>
	
	<select id="selectLoginById" resultType="member.dto.Member" parameterType="string">
		SELECT * FROM member
		WHERE user_id = #{userId }
	</select>
	
</mapper>